[package]
name = "tauri-plugin-notification"
version = "2.3.3"
description = "Send desktop and mobile notifications on your Tauri application."
edition = "2021"
license = "MIT OR Apache-2.0"
rust-version = "1.77.2"
repository = "https://github.com/tinyhumansai/tauri-plugin-notification"
links = "tauri-plugin-notification"

[package.metadata.docs.rs]
targets = ["x86_64-unknown-linux-gnu", "x86_64-linux-android"]

[package.metadata.platforms.support]
windows = { level = "full", notes = "Only works for installed apps. Shows powershell name & icon in development." }
linux = { level = "full", notes = "" }
macos = { level = "full", notes = "" }
android = { level = "full", notes = "" }
ios = { level = "full", notes = "" }

[build-dependencies]
tauri-plugin = { path = "../tauri-cef/crates/tauri-plugin", features = ["build"] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { path = "../tauri-cef/crates/tauri", default-features = false }
log = "0.4"
thiserror = "2"
rand = "0.9"
time = { version = "0.3", features = ["serde", "parsing", "formatting"] }
url = { version = "2", features = ["serde"] }
serde_repr = "0.1"

[target.'cfg(target_os = "ios")'.dependencies]
tauri = { path = "../tauri-cef/crates/tauri", default-features = false, features = ["wry"] }

[target."cfg(windows)".dependencies]
win7-notifications = { version = "0.4.5", optional = true }
windows-version = { version = "0.1", optional = true }

[target."cfg(any(target_os = \"macos\", windows, target_os = \"linux\", target_os = \"dragonfly\", target_os = \"freebsd\", target_os = \"openbsd\", target_os = \"netbsd\"))".dependencies]
notify-rust = "4.11"

[features]
windows7-compat = ["win7-notifications", "windows-version"]
